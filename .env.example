# Single Supabase project hosting both schemas (wbgts + noise-meters).
# Copy the values from either alerts repo's .env — they point at the same project.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Optional overrides
# PORT=5178
# WBGT_SCHEMA=wbgts
# NOISE_SCHEMA=noise-meters
