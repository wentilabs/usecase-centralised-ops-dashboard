{
  "name": "usecase-wohhup-ops-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local read-only floating-cards dashboard for WBGT + Noise project configs (Supabase ground truth).",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
